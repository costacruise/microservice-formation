dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - echo 'no tests' 

deployment:
  production:
    branch: master
    commands:
      - aws s3 sync config s3://microservice-formation --delete 
